# RU 152-ФЗ personal-data regime, layered over core.
name = "ru-152fz"
content_hash = "be8f12bd57033f58a65a34e2bea9fed04ef2a0a9b78f0de59ee73f47e28947c3"
extends = ["core"]
detectors = ["regex_checksum", "dual_ner"]
# Advisory only — not part of the manifest schema; parse_manifest ignores it and
# no code path reads it yet (drives residency selection at deploy level later).
data_residency = "ru"

[policy]
block_payloads = true
dlp_guard = true
oracle_mode = "any_local_finding"

[policy.fail_policy]
pre_pass_down = "fail-closed"
