# GitHub CODEOWNERS — see https://docs.github.com/articles/about-code-owners
# Required-approval sections (e.g. [Security][1]) can be added once project roles are mapped.
# Team handles below (@corp/compliance, @corp/ops, @corp/security-eng) are PLACEHOLDERS —
# the DRI wires the real GitHub team handles once project roles are mapped.

* @jlam-err

# Plan + design docs require DRI review
/docs/plans/  @jlam-err

# Helm chart changes need ops review (assign when ops owner is known)
/helm/        @jlam-err

# Sanitizer core — security-sensitive, DRI must review
/src/corp_llm_gateway/sanitizer/  @jlam-err

# Review split (plan D6): profile *data* vs detector *code*, enforced by tooling
# not convention. GitHub lets a later match override an earlier one, so the code
# carve-outs below win over the profiles/ data rule.

# Profile data bundles (profile.toml, term lists, replace.md, allowlists) —
# compliance/ops own policy content (the fast data-review lane, no code path).
/src/corp_llm_gateway/profiles/  @corp/compliance @corp/ops

# Executable detector code on the leak-critical egress path — security-eng owns
# detector algorithms + the detector registry (the slow code-review lane; ADR-001
# keeps arbitrary code off egress). Listed AFTER profiles/ to override for these.
/src/corp_llm_gateway/detectors/  @corp/security-eng
/src/corp_llm_gateway/profiles/registry.py  @corp/security-eng
